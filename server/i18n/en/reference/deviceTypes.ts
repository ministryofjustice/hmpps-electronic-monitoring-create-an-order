import DeviceTypes from '../../../types/i18n/reference/deviceTypes'

const deviceTypes: DeviceTypes = {
  FITTED: 'A fitted GPS tag',
  NON_FITTED: 'A non-fitted device',
}

export default deviceTypes
