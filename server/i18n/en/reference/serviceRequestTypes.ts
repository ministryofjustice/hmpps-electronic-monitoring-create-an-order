import ServiceRequestTypes from '../../../types/i18n/reference/serviceRequestTypes'

const serviceRequestTypes: ServiceRequestTypes = {
  REINSTALL_AT_DIFFERENT_ADDRESS: 'I need monitoring equipment installed at a new address',
  REINSTALL_DEVICE: 'I need monitoring equipment reinstalled or checked',
  REVOCATION: 'I need to end all monitoring for the device wearer',
  VARIATION: 'I need to change something else in the form',
}

export default serviceRequestTypes
