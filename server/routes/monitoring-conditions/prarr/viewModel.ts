import { ValidationResult } from '../../../models/Validation'
import { MonitoringConditions } from '../model'

const constructModel = (data: MonitoringConditions, errors: ValidationResult) => {
  return {}
}

export default constructModel
